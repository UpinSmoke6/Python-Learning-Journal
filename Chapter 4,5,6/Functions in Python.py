# Creating your own function
def greet(): #defining the function
    name = input("Enter your name: ")
    print(f"Hello, {name}!")

greet() # Shorter functions, the better Definitions only work within function itself.